import { MainLayout } from '@/components/common/MainLayout';
import { AboutView } from '@/components/view';

export default function About() {
    return (
        <MainLayout>
            <AboutView />
        </MainLayout>
    );
}
